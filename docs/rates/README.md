# Rates v1 — Auth repo

No direct code changes are required for Rates v1.

This folder exists so the project documentation is discoverable across the three repos.

See:
- booking-backend: `docs/rates/*`
- booking-frontend: `docs/rates/*`

